import { GlDirective } from './gl.directive';

describe('GlDirective', () => {
  it('should create an instance', () => {
    const directive = new GlDirective();
    expect(directive).toBeTruthy();
  });
});
